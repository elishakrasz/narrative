input UpdateCommunityInput {
  name: String!
  picture: Upload
  category: String!
  country: String!
  latitude: Float!
  longitude: Float!
}

type Mutation {
    updateCommunity(communityId: String!, input: UpdateCommunityInput!): Boolean!
}